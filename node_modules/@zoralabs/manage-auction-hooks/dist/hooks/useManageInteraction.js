"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useManageInteraction = void 0;
const units_1 = require("@ethersproject/units");
const simple_wallet_provider_1 = require("@zoralabs/simple-wallet-provider");
const react_1 = require("react");
const utils_1 = require("ethers/lib/utils");
const useThemeConfig_1 = require("../hooks/useThemeConfig");
const useEthAmountInput_1 = require("../components/useEthAmountInput");
const useAuctionHouseHooksContext_1 = require("../hooks/useAuctionHouseHooksContext");
const addresses_1 = require("../utils/addresses");
const types_1 = require("../types");
const useContractTransaction_1 = require("./useContractTransaction");
const useManageInteraction = (auction, setError) => {
    const { getString } = useThemeConfig_1.useThemeConfig();
    const { account } = simple_wallet_provider_1.useWeb3Wallet();
    const { auctionHouse, auctionId } = useAuctionHouseHooksContext_1.useAuctionHouseHooksContext();
    const auctionHasEnded = auction.firstBidTime.gt("0") &&
        new Date().getTime() / 1000 >
            auction.firstBidTime.add(auction.duration).toNumber();
    const { handleTx: handleCancelTx, txStatus: cancelTxStatus } = useContractTransaction_1.useContractTransaction(types_1.ActionType.CANCEL_AUCTION);
    const { handleTx: handleSetReserveTx, txStatus: setReserveTxStatus } = useContractTransaction_1.useContractTransaction(types_1.ActionType.UPDATE_RESERVE);
    const { handleTx: handleEndAuctionTx, txStatus: endAuctionTxStatus } = useContractTransaction_1.useContractTransaction(types_1.ActionType.END_AUCTION);
    const { ethValue, input } = useEthAmountInput_1.useEthAmountInput({
        hasMinPrecision: true,
        label: getString("UPDATE_RESERVE_PRICE_PRICE_LABEL"),
    });
    const handleCancelAuction = react_1.useCallback(async () => {
        setError(undefined);
        if (!auctionHouse || auctionId === null) {
            setError("No auction found");
            return;
        }
        try {
            await handleCancelTx(auctionHouse === null || auctionHouse === void 0 ? void 0 : auctionHouse.cancelAuction(auctionId));
        }
        catch (error) {
            console.error(error);
            setError(`Error cancelling auction: ${error.message}`);
        }
    }, [auctionHouse, auctionId, setError, handleCancelTx]);
    const handleEndAuction = react_1.useCallback(async () => {
        setError(undefined);
        if (!auctionHouse || auctionId === null) {
            setError("No auction found");
            return;
        }
        try {
            await handleEndAuctionTx(auctionHouse === null || auctionHouse === void 0 ? void 0 : auctionHouse.endAuction(auctionId));
        }
        catch (error) {
            console.error(error);
            setError(`Error ending auction: ${error.message}`);
        }
    }, [auctionHouse, auctionId, setError, handleEndAuctionTx]);
    const handleUpdateReservePrice = react_1.useCallback(async () => {
        setError(undefined);
        if (!auctionHouse || auctionId === null) {
            setError("No auction found");
            return;
        }
        if (ethValue) {
            try {
                await handleSetReserveTx(auctionHouse === null || auctionHouse === void 0 ? void 0 : auctionHouse.setAuctionReservePrice(auctionId, units_1.parseEther(ethValue)));
            }
            catch (error) {
                console.error(error);
                setError(`Error cancelling auction: ${error.message}`);
            }
        }
        else {
            setError("Invalid reserve price");
        }
    }, [setError, auctionHouse, auctionId, ethValue]);
    const isTokenOwner = utils_1.isAddress(account) &&
        addresses_1.addressesMatch(account, auction.tokenOwner);
    return {
        auctionHasEnded,
        cancelTxStatus,
        endAuctionTxStatus,
        ethValue,
        handleCancelAuction,
        handleEndAuction,
        handleUpdateReservePrice,
        input,
        isTokenOwner,
        setReserveTxStatus,
    };
};
exports.useManageInteraction = useManageInteraction;
